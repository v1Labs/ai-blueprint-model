# Examples

This directory contains blueprint-local examples for the `asset-blueprint` package.

- Use descriptive file names tied to the scenario or channel pair.
- Keep examples aligned with `constraints.md`, `content.md`, and `style.md`.
- Add counter-examples only when they clarify a likely failure mode.
