# basic schema file guidance

Each file in this directory explains the recommended shape and role of the corresponding blueprint file in a `basic` schema package.

Use these guides to keep blueprint packages:
- readable in plain markdown
- easy to author without special tooling
- consistent across repositories and teams

The guidance is intentionally descriptive rather than rigidly validating. Blueprint authors should adapt section depth to the artifact, while preserving the core responsibility of each file.
