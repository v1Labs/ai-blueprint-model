# asset schema file guidance

Each file in this directory explains the recommended shape and role of the corresponding blueprint file in an `asset` schema package.

The `asset` schema builds on `basic` and adds clearer separation between:
- content sourcing and generation guidance
- visual, stylistic, and brand direction

Use these guides to keep artifact-generation blueprints modular, readable, and easy for both humans and AI systems to navigate.
